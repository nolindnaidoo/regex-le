#!/usr/bin/env python
"""Every slash here is a path or a division, and none is a regex."""

RATE = total / count / 2
HALF = 1 / 2
ROOT = "/var/log/app.log"
URL = "https://example.com/path/to/resource"


def share(a, b):
    return a / b
